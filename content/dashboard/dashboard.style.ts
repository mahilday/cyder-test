import { headerHeight } from "@cd/lib/general-sizes.data";

export const sideSectionWrapper: React.CSSProperties = {
  borderRadius: "8px",
  maxHeight: "88dvh",
  width: "30%",
  overflowY: "auto",
  position: "sticky",
  top: headerHeight + 20,
};
