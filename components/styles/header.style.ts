import { headerHeight } from "@cd/lib/general-sizes.data";

export const headerWrapper = {
  display: "flex",
  justifyContent: "space-between",
  alignItems: "center",
  height: headerHeight,
  boxShadow: "3px 1px 10px rgba(0, 0, 0, 0.15)",
  width: "100%",
  padding: "0 10px",
  borderRadius: "100px",
};

export const bellIconWrapperStyle = {
  borderRadius: "50%",
  padding: "6px 8px",
};
